---
title: 何謂主控件節流？ | Microsoft Docs
ms.custom: ''
ms.date: 06/08/2017
ms.prod: biztalk-server
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- host throttling, outbound
- host throttling, inbound
- host throttling, about host throttling
ms.assetid: 36d1818b-c8a2-4f23-bfb3-c034ee242f69
caps.latest.revision: ''
author: MandiOhlinger
ms.author: mandia
manager: anneta
ms.openlocfilehash: c4730a93b6491f53fc06004ca8bb0dcb0d970cc7
ms.sourcegitcommit: 8418b1a8f38b7f56979cd6e203f0b591e2f40fe1
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 03/28/2018
---
# <a name="what-is-host-throttling"></a><span data-ttu-id="cf00a-103">何謂主控件節流？</span><span class="sxs-lookup"><span data-stu-id="cf00a-103">What Is Host Throttling?</span></span>
<span data-ttu-id="cf00a-104">大部分在 BizTalk Server 上執行的處理會在稱為 BizTalk Server 主控件執行個體的邏輯實體中發生，當做 Windows 服務或外掛式主控件程序在 BizTalk Server 上執行。</span><span class="sxs-lookup"><span data-stu-id="cf00a-104">Most of the processing that takes place on a BizTalk server occurs within a logical entity known as a BizTalk Server host instance, which is a process running as a Windows service or an isolated host process on the BizTalk server.</span></span> <span data-ttu-id="cf00a-105">為管理主控件執行個體程序對資源的使用，BizTalk Server 會利用一種透過主控件執行個體管理訊息流程和處理的可調式節流機制。</span><span class="sxs-lookup"><span data-stu-id="cf00a-105">To manage the use of resources by a host instance process, BizTalk Server utilizes an adjustable throttling mechanism that governs the flow and processing of messages through a host instance.</span></span>  
  
 <span data-ttu-id="cf00a-106">此節流機制會節制主控件執行個體的工作負載，以確保工作負載不超過主控件執行個體或任何下游主控件執行個體的容量。</span><span class="sxs-lookup"><span data-stu-id="cf00a-106">The throttling mechanism moderates the workload of the host instance to ensure that the workload does not exceed the capacity of the host instance or any downstream host instances.</span></span> <span data-ttu-id="cf00a-107">此節流機制也會防止爭用內容的情況，這種情況會降低主控件執行個體程序或其他系統程序的整體效能。</span><span class="sxs-lookup"><span data-stu-id="cf00a-107">The throttling mechanism also prevents a condition known as resource contention that can lower the overall performance of the host instance process or other system processes.</span></span> <span data-ttu-id="cf00a-108">當一或多個程序耗用有限的資源，對本身和 (或) 其他程序造成損害時，就會發生爭用內容。</span><span class="sxs-lookup"><span data-stu-id="cf00a-108">Resource contention occurs when one or more processes consume a limited resource to the detriment of themselves and/or another process.</span></span> <span data-ttu-id="cf00a-109">例如，過度使用記憶體或執行緒可能造成記憶體配置失敗或高度執行緒內容切換，有可能會影響程序效能。</span><span class="sxs-lookup"><span data-stu-id="cf00a-109">For example, the consumption of excessive memory or threads can lead to memory allocation failure or high thread context-switches, which can impact the performance of the process.</span></span> <span data-ttu-id="cf00a-110">類似這種爭用內容的情況可能對 BizTalk Server 的整體效能造成損害。</span><span class="sxs-lookup"><span data-stu-id="cf00a-110">Resource contention like this can be detrimental to the overall performance of BizTalk Server.</span></span>  
  
 <span data-ttu-id="cf00a-111">主控件節流機制還可偵測是否未充分使用可用的資源。</span><span class="sxs-lookup"><span data-stu-id="cf00a-111">The host throttling mechanism also detects when available resources are being underutilized.</span></span> <span data-ttu-id="cf00a-112">若未充分使用可用資源，節流機制會允許主控件執行個體處理額外訊息。</span><span class="sxs-lookup"><span data-stu-id="cf00a-112">If available resources are underutilized then the throttling mechanism allows additional messages to be processed by a host instance.</span></span> <span data-ttu-id="cf00a-113">若可用資源處於過度使用或未充分使用的狀態，主控件節流機制會持續監控，並且透過主控件執行個體調整訊息流程。</span><span class="sxs-lookup"><span data-stu-id="cf00a-113">The host throttling mechanism continually monitors if available resources are being over or underutilized and adjusts message flow through the host instance accordingly.</span></span>  
  
 <span data-ttu-id="cf00a-114">BizTalk Server 主控件節流機制可協助確保系統在最佳且可承受的層級作業。</span><span class="sxs-lookup"><span data-stu-id="cf00a-114">The BizTalk Server host throttling mechanism helps to ensure that the system operates at an optimal and sustainable level.</span></span>  
  
 <span data-ttu-id="cf00a-115">主控件節流組態參數是在 [BizTalk Server 管理] 主控台中，以每個主控件為基礎而設定的。</span><span class="sxs-lookup"><span data-stu-id="cf00a-115">Host throttling configuration parameters are set on a per host basis in the BizTalk Server Administration console.</span></span> <span data-ttu-id="cf00a-116">請注意，為主控件設定的節流組態參數，可以套用至儲存在對應主控件執行個體中的任何或所有接收處理常式、傳送處理常式或協調流程。</span><span class="sxs-lookup"><span data-stu-id="cf00a-116">Note that the throttling configuration parameters that are set for the host apply to any or all receive handlers, send handlers, or orchestrations that are housed in the corresponding host instances.</span></span> <span data-ttu-id="cf00a-117">若您要設定只能套用至特定接收處理常式、傳送處理常式或協調流程的節流參數，則應該執行下列動作：</span><span class="sxs-lookup"><span data-stu-id="cf00a-117">If you want to set throttling parameters that will apply only to a specific receive handler, send handler, or orchestration then you should do the following:</span></span>  
  
-   <span data-ttu-id="cf00a-118">建立一個新的主控件並適當地設定節流參數。</span><span class="sxs-lookup"><span data-stu-id="cf00a-118">Create a new host and set the throttling parameters appropriately.</span></span>  
  
-   <span data-ttu-id="cf00a-119">設定要在此主控件中執行的接收處理常式、傳送處理常式或協調流程。</span><span class="sxs-lookup"><span data-stu-id="cf00a-119">Configure the receive handler, send handler, or orchestration to run in this host.</span></span>  
  
-   <span data-ttu-id="cf00a-120">建立這個主控件的一或多個執行個體，以在您的 BizTalk Server 上執行。</span><span class="sxs-lookup"><span data-stu-id="cf00a-120">Create one or more instances of this host to run on your BizTalk server(s).</span></span>  
  
## <a name="inbound-host-throttling"></a><span data-ttu-id="cf00a-121">輸入主控件節流</span><span class="sxs-lookup"><span data-stu-id="cf00a-121">Inbound host throttling</span></span>  
 <span data-ttu-id="cf00a-122">輸入主控件節流，也就 *訊息發佈節流* 在 BizTalk Server 中，會套用至主控件執行個體包含接收配接器或協調流程發佈訊息至 MessageBox 資料庫。</span><span class="sxs-lookup"><span data-stu-id="cf00a-122">Inbound host throttling, also known as *message publishing throttling* in BizTalk Server, is applied to host instances that contain receive adapters or orchestrations that publish messages to the MessageBox database.</span></span> <span data-ttu-id="cf00a-123">輸入主控件節流情況會在下列情況下觸發：</span><span class="sxs-lookup"><span data-stu-id="cf00a-123">An inbound host throttling condition can be triggered under the following conditions:</span></span>  
  
-   <span data-ttu-id="cf00a-124">記憶體、 執行緒數目或主控件執行個體所使用的資料庫連線數目超過節流閾值中定義 **設定儀表板** BizTalk 群組中，選取 **設定**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-124">The amount of memory, the number of threads, or the number of database connections used by the host instance exceeds the throttling thresholds defined in **Settings Dashboard** available in the BizTalk Group and selecting **Settings**.</span></span> <span data-ttu-id="cf00a-125">這些值是可測量的效能監控計數器底下 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-125">These values are measurable with performance monitor counters available under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-126">下游主控件無法處理已發佈的訊息。</span><span class="sxs-lookup"><span data-stu-id="cf00a-126">Downstream hosts are unable to process the messages that are published.</span></span> <span data-ttu-id="cf00a-127">這會增加的值 **訊息計數和在 DB** 參數。</span><span class="sxs-lookup"><span data-stu-id="cf00a-127">This increases the value of the **Message count in DB** parameter.</span></span> <span data-ttu-id="cf00a-128">臨界值 **訊息計數和在 DB** 值觸發程序的節流條件是可在設定 **主機** 選項 **設定儀表板**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-128">The threshold at which the **Message count in DB** value triggers a throttling condition is configurable on the **Hosts** option in **Settings Dashboard**.</span></span> <span data-ttu-id="cf00a-129">在資料庫中的訊息計數可以測量與 **資料庫大小** 計數器底下 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-129">The message count in database can be measured with the **Database Size** counter under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-130">**訊息發佈內送速率** 主控件執行個體超過 **訊息發佈外寄速率\*** 指定 **速率增加因數** 值。</span><span class="sxs-lookup"><span data-stu-id="cf00a-130">The **Message publishing incoming rate** for the host instance exceeds the **Message publishing outgoing rate\*** the specified **Rate overdrive factor** value.</span></span> <span data-ttu-id="cf00a-131">**速率增加因數** 值以定義 **設定儀表板**, ，**主機** 然後 **根據速率的節流**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-131">The **Rate overdrive factor** value is defined in **Settings Dashboard**, **Hosts** and then **Rate-Based Throttling**.</span></span> <span data-ttu-id="cf00a-132">訊息發佈的輸入和輸出速率可以測量與對應的效能監視器計數器底下 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-132">The message publishing incoming and outgoing rates can be measured with the corresponding performance monitor counters under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-133">已修改預設節流行為。</span><span class="sxs-lookup"><span data-stu-id="cf00a-133">The default throttling behavior has been modified.</span></span> <span data-ttu-id="cf00a-134">[針對 BizTalk Server 效能調整使用設定儀表板](../core/using-settings-dashboard-for-biztalk-server-performance-tuning.md)說明影響節流行為的不同值。</span><span class="sxs-lookup"><span data-stu-id="cf00a-134">[Using Settings Dashboard for BizTalk Server Performance Tuning](../core/using-settings-dashboard-for-biztalk-server-performance-tuning.md) describes the different values that impact throttling behavior.</span></span>  
  
 <span data-ttu-id="cf00a-135">根據節流情況嚴重性的不同，會執行下列動作：</span><span class="sxs-lookup"><span data-stu-id="cf00a-135">Depending on the severity of the throttling condition, the following actions are taken:</span></span>  
  
-   <span data-ttu-id="cf00a-136">實作主控件執行個體處理邏輯中的漸進延遲。</span><span class="sxs-lookup"><span data-stu-id="cf00a-136">A progressive delay in the processing logic of the host instance is implemented.</span></span> <span data-ttu-id="cf00a-137">當「結束點管理員」(EPM) 執行緒收到來自傳輸配接器的訊息批次時，和 (或) EPM 提交一個要發佈的訊息批次至 MessageBox 資料庫時，可能會實作延遲。</span><span class="sxs-lookup"><span data-stu-id="cf00a-137">The delay may be implemented when an End Point Manager (EPM) thread receives a batch of messages from the transport adapter, and/or when the EPM submits a batch of messages to be published into the MessageBox database.</span></span> <span data-ttu-id="cf00a-138">處理延遲的持續時間以及持續時間遞增的速率會隨著節流情況的嚴重性增加。</span><span class="sxs-lookup"><span data-stu-id="cf00a-138">Both the duration of the processing delay and the rate at which the duration is incremented scale with the severity of the throttling condition.</span></span>  
  
-   <span data-ttu-id="cf00a-139">「結束點管理員」(EPM) 可使用的執行緒數目是有限制的。</span><span class="sxs-lookup"><span data-stu-id="cf00a-139">The number of threads that are available to the End Point Manager (EPM) is restricted.</span></span> <span data-ttu-id="cf00a-140">EPM 接收來自配接器的訊息批次，並發佈訊息至 MessageBox 資料庫。</span><span class="sxs-lookup"><span data-stu-id="cf00a-140">The EPM receives batches of messages from adapters and publishes the messages to the MessageBox database.</span></span> <span data-ttu-id="cf00a-141">依照預設，EPM 設定為每一 CPU 使用 20 個執行緒。</span><span class="sxs-lookup"><span data-stu-id="cf00a-141">By default, the EPM is configured to use 20 threads per CPU.</span></span> <span data-ttu-id="cf00a-142">若主控件節流機制偵測到輸入處理的負荷情況非常高，則會暫時減少 EPM 可用的執行緒數目，直到負荷情況已排除。</span><span class="sxs-lookup"><span data-stu-id="cf00a-142">If the host throttling mechanism detects a stress condition for inbound processing then it can temporarily reduce the number of threads available to the EPM until the stress condition is eliminated.</span></span> <span data-ttu-id="cf00a-143">EPM 不能處理來自傳輸配接器的訊息或傳遞訊息批次到 MessageBox 資料庫，除非某個 EPM 執行緒可對輸入訊息批次提供服務。</span><span class="sxs-lookup"><span data-stu-id="cf00a-143">The EPM cannot process messages from transport adapters or deliver message batches to the MessageBox database unless an EPM thread is available to service the inbound message batch.</span></span>  
  
-   <span data-ttu-id="cf00a-144">當適用時，降低記憶體和其他資源的使用率。</span><span class="sxs-lookup"><span data-stu-id="cf00a-144">The use of memory and other resources is reduced as applicable.</span></span> <span data-ttu-id="cf00a-145">BizTalk Server 可以傳送指示給其他服務類別，藉由凍結執行排程、縮小記憶體快取大小以及限制需要大量記憶體的執行緒之使用量，以限制記憶體使用率。</span><span class="sxs-lookup"><span data-stu-id="cf00a-145">BizTalk Server can send instructions to other service classes to limit memory use by dehydrating running schedules, shrinking memory cache size, and by limiting the usage of memory-intensive threads.</span></span>  
  
 <span data-ttu-id="cf00a-146">**從配接器到 MessageBox 的輸入的訊息流程**</span><span class="sxs-lookup"><span data-stu-id="cf00a-146">**Inbound message flow from Adapter to MessageBox**</span></span>  
  
 <span data-ttu-id="cf00a-147">![輸入從配接器到 MessageBox 的訊息流程](../core/media/inboundmsgflow.gif "InboundMsgFlow")</span><span class="sxs-lookup"><span data-stu-id="cf00a-147">![Inbound Message flow from Adapter to MessageBox](../core/media/inboundmsgflow.gif "InboundMsgFlow")</span></span>  
  
## <a name="outbound-host-throttling"></a><span data-ttu-id="cf00a-148">輸出主控件節流</span><span class="sxs-lookup"><span data-stu-id="cf00a-148">Outbound host throttling</span></span>  
 <span data-ttu-id="cf00a-149">輸出主控件節流，也就 *訊息處理節流* 在 BizTalk Server 中，會套用至包含協調流程或傳送配接器的接收、 傳遞或處理已發佈至 MessageBox 的訊息的主控件執行個體。</span><span class="sxs-lookup"><span data-stu-id="cf00a-149">Outbound host throttling, also known as *message processing throttling* in BizTalk Server, is applied to host instances that contain orchestrations or send adapters that receive and deliver or process messages that have been published to the MessageBox.</span></span> <span data-ttu-id="cf00a-150">輸出主控件節流情況會在下列情況下觸發：</span><span class="sxs-lookup"><span data-stu-id="cf00a-150">An outbound host throttling condition can be triggered under the following conditions:</span></span>  
  
-   <span data-ttu-id="cf00a-151">記憶體、 執行緒數目或主控件執行個體所使用的資料庫連線數目超過節流閾值中定義 **資源型節流** 中 **設定儀表板**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-151">The amount of memory, the number of threads, or the number of database connections used by the host instance exceeds the throttling thresholds defined in **Resource-Based Throttling** in **Settings Dashboard**.</span></span> <span data-ttu-id="cf00a-152">這些值是可測量的效能監控計數器底下 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-152">These values are measurable with performance monitor counters available under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-153">**訊息傳遞內送速率** 主控件執行個體超過 **訊息傳遞外寄速率** \* 指定 **速率增加因數** 值。</span><span class="sxs-lookup"><span data-stu-id="cf00a-153">The **Message delivery incoming rate** for the host instance exceeds the **Message delivery outgoing rate** \* the specified **Rate overdrive factor** value.</span></span> <span data-ttu-id="cf00a-154">**速率增加因數** 值定義在 **根據速率的節流** 索引標籤中 **設定儀表板**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-154">The **Rate overdrive factor** value is defined on the **Rate-Based Throttling** tab in **Settings Dashboard**.</span></span> <span data-ttu-id="cf00a-155">訊息傳遞輸入和輸出速率可以測量與對應的效能監控計數器底下 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-155">The message delivery incoming and outgoing rates can be measured with the corresponding performance monitor counters under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-156">同時處理的主控件執行個體的訊息數目超過 **程序中的每一 CPU 訊息** \* 方塊上可用的 Cpu 數目。</span><span class="sxs-lookup"><span data-stu-id="cf00a-156">The number of messages being processed concurrently by the host instance exceeds the **In-process messages per CPU** \* the number of CPUs available on the box.</span></span> <span data-ttu-id="cf00a-157">**內含式訊息** 臨界值定義在 **資源型節流** 索引標籤中 **設定儀表板**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-157">The **In-process messages** threshold is defined on the **Resource-Based Throttling** tab in **Settings Dashboard**.</span></span> <span data-ttu-id="cf00a-158">同時處理的主控件執行個體的訊息數目可以測量與 **內含式訊息計數** 下的效能計數器 **Messageagent** 效能物件類別。</span><span class="sxs-lookup"><span data-stu-id="cf00a-158">The number of messages being processed concurrently by the host instance can be measured with the **In-process message count** performance counter under the **BizTalk:Message Agent** performance object category.</span></span>  
  
-   <span data-ttu-id="cf00a-159">已修改預設節流行為。</span><span class="sxs-lookup"><span data-stu-id="cf00a-159">The default throttling behavior has been modified.</span></span> <span data-ttu-id="cf00a-160">[針對 BizTalk Server 效能調整使用設定儀表板](../core/using-settings-dashboard-for-biztalk-server-performance-tuning.md)說明影響節流行為的不同值。</span><span class="sxs-lookup"><span data-stu-id="cf00a-160">[Using Settings Dashboard for BizTalk Server Performance Tuning](../core/using-settings-dashboard-for-biztalk-server-performance-tuning.md) describes the different values that impact throttling behavior.</span></span>  
  
 <span data-ttu-id="cf00a-161">根據節流情況嚴重性的不同，會執行下列動作：</span><span class="sxs-lookup"><span data-stu-id="cf00a-161">Depending upon the severity of the throttling condition, the following actions are taken:</span></span>  
  
-   <span data-ttu-id="cf00a-162">在傳遞訊息到輸出傳輸配接器或用來處理訊息的協調流程引擎之前，會實作主控件執行個體處理邏輯中的漸進延遲。</span><span class="sxs-lookup"><span data-stu-id="cf00a-162">A progressive delay in the processing logic of the host instance is implemented before delivering the messages to the outbound transport adapter or the orchestration engine for processing the messages.</span></span> <span data-ttu-id="cf00a-163">處理延遲的持續時間以及持續時間遞增的速率會隨著節流情況的嚴重性增加。</span><span class="sxs-lookup"><span data-stu-id="cf00a-163">Both the duration of the processing logic delay and the rate at which the duration is incremented scale with the severity of the throttling condition.</span></span>  
  
-   <span data-ttu-id="cf00a-164">記憶體中的佇列所能保存的訊息數目是有限制的。</span><span class="sxs-lookup"><span data-stu-id="cf00a-164">The number of messages that can be held by the in-memory queue is limited.</span></span> <span data-ttu-id="cf00a-165">記憶體中的佇列，是從 MessageBox 傳遞訊息到「訊息代理程式」，再從「訊息代理程式」傳遞訊息到 XLANG 與傳送配接器時的暫時預留位置。</span><span class="sxs-lookup"><span data-stu-id="cf00a-165">The in-memory queue serves as a temporary placeholder for delivering messages from the MessageBox to the Message Agent which in turn delivers messages to XLANG and send adapters.</span></span> <span data-ttu-id="cf00a-166">依照預設，記憶體中的佇列設定為每一 CPU 保留 100 個訊息。</span><span class="sxs-lookup"><span data-stu-id="cf00a-166">By default, the in-memory queue is set to hold 100 messages per CPU.</span></span> <span data-ttu-id="cf00a-167">當佇列已滿時，除非釋放記憶體中的佇列，否則不會再從 MessageBox 清除訊息佇列。</span><span class="sxs-lookup"><span data-stu-id="cf00a-167">When the queue is full, no more messages are de-queued from the MessageBox until the in-memory queue is freed up.</span></span>  
  
-   <span data-ttu-id="cf00a-168">「訊息代理程式」執行緒集區大小是有限制的。</span><span class="sxs-lookup"><span data-stu-id="cf00a-168">The size of the Message Agent thread pool is limited.</span></span> <span data-ttu-id="cf00a-169">藉由限制「訊息代理程式」執行緒集區的大小，主控件節流機制可有效降低傳遞至 XLANG 與配接器的訊息數量。</span><span class="sxs-lookup"><span data-stu-id="cf00a-169">By limiting the Message Agent thread pool size, the host throttling mechanism effectively reduces the amount of messages that are delivered to XLANG and adapters.</span></span>  
  
     <span data-ttu-id="cf00a-170">預設訊息代理程式執行緒集區大小可以變更來修改 **最大值的引擎執行緒** 值 **一般** 索引標籤中 **設定儀表板**。</span><span class="sxs-lookup"><span data-stu-id="cf00a-170">The default Message Agent thread pool size can be modified by changing the **Maximum engine threads** value on the **General** tab in **Settings Dashboard**.</span></span> <span data-ttu-id="cf00a-171">如需有關如何修改此值的詳細資訊，請參閱[如何修改一般設定](../core/how-to-modify-general-settings.md)。</span><span class="sxs-lookup"><span data-stu-id="cf00a-171">For more information about modifying this value, see [How to Modify General Settings](../core/how-to-modify-general-settings.md).</span></span>  
  
-   <span data-ttu-id="cf00a-172">當適用時，降低記憶體和其他資源的使用率。</span><span class="sxs-lookup"><span data-stu-id="cf00a-172">The use of memory and other resources is reduced as applicable.</span></span> <span data-ttu-id="cf00a-173">BizTalk Server 可以傳送指示給其他服務類別，藉由凍結執行排程、縮小記憶體快取大小以及限制需要大量記憶體的執行緒之使用量，以限制記憶體使用率。</span><span class="sxs-lookup"><span data-stu-id="cf00a-173">BizTalk Server can send instructions to other service classes to limit memory use by dehydrating running schedules, shrinking memory cache size, and by limiting the usage of memory intensive threads.</span></span>  
  
 <span data-ttu-id="cf00a-174">**從 MessageBox 到配接器與 XLANG 的輸出訊息流程**</span><span class="sxs-lookup"><span data-stu-id="cf00a-174">**Outbound message flow from MessageBox to Adapters and XLANG**</span></span>  
  
 <span data-ttu-id="cf00a-175">![配接器和 XLANG 的輸出訊息流程](../core/media/outboundmsgflow.gif "OutboundMsgFlow")</span><span class="sxs-lookup"><span data-stu-id="cf00a-175">![Outbound message flow to Adapters and XLANG](../core/media/outboundmsgflow.gif "OutboundMsgFlow")</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cf00a-176">另請參閱</span><span class="sxs-lookup"><span data-stu-id="cf00a-176">See Also</span></span>  
 <span data-ttu-id="cf00a-177">[BizTalk Server 如何實作主控件節流](../core/how-biztalk-server-implements-host-throttling.md) </span><span class="sxs-lookup"><span data-stu-id="cf00a-177">[How BizTalk Server Implements Host Throttling](../core/how-biztalk-server-implements-host-throttling.md) </span></span>  
 [<span data-ttu-id="cf00a-178">使用設定儀表板，以便讓 BizTalk Server 效能調整</span><span class="sxs-lookup"><span data-stu-id="cf00a-178">Using Settings Dashboard for BizTalk Server Performance Tuning</span></span>](../core/using-settings-dashboard-for-biztalk-server-performance-tuning.md)